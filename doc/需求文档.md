一、产品定位
名字：中文：图标王制作器，英文：KiconCreator，简称KIcon
版本：V2.02
零安装、全程序在浏览器运行的图标制作工具，不需要后端支持，只包含js和html，不保存、不需要任何localStorage。
浏览器：chrome、edge
二、功能需求
参考，具备并超出www.fuwa.org/tools/icon-generator.html、tool.mastermao.com/zh/tool/icon-maker的功能。

（一）预览预设下载栏：包含预览、预设、下载三个模块。
    1. [模块]预览：跟随参数变化，同步展示当前参数配置下的图标结果。
        1.1. 只显示当前预览，不显示多尺寸预览，暂无其它功能。预览宽度与模块宽度相同。预览尺寸=画布尺寸=当前的下载设置的尺寸，画布中显示时候做等比例缩放来适应容器尺寸。
            原位缩放：鼠标悬浮时候通过滚轮缩放进入缩放模式，缩放模式下可左键按下拖动，滚轮缩放，缩放模式下，左键双击复位并退出缩放模式。
        1.2. 竖屏情况下，保持位置在视口上方浮动固定，最大高度不超过视口1/3。
        1.3. [组件]放大按钮：多栏模式下，点击后左栏变成视口宽度的一半，这样预览窗口跟着扩大。
        1.4. 模态预览器：预览不处于缩放模式时候，双击预览图片，预览模式是进入模态查看器。图片按下载设置的真实尺寸1：1显示，可自由放大拖动预览图片。辅助线如果开启，应跟随缩放和移动。模态查看器内模态内双击复位平移 + 缩放。ESC、点击关闭按钮均可退出模态查看器。
        1.5.[组件]辅助线下拉框：显示无、十字、米子、均分井字辅助线。辅助线为半透明灰色，线宽1px，暂不支持拖动、缩放、颜色和交互。
        1.6.[组件]安全边距辅助线：勾选显示安全边距，可设置百分比参数，默认位于10%的画布宽度，线宽1px，半透明灰色，随画布缩放和移动。
        1.7 画布背景色为灰白格子，画布背景不导出不下载。
        1.8 应确保预览效果即导出效果（除画布背景不导出）。若形状为无按白色或透明（勾选透明时候）导出背景。
    2. [模块]预设和历史：包含预设和历史标签组
        2.1 [标签]预设：包含包括内容、背景在内的预设。开发时候内置为JS常量数组。
            [组件]导入、导出：预设可导出和加载，保存当前为预设，加载后在预设列表中显示导入的预取样式。导入导出做参数合法性校验，不做版本校验，json多的参数抛弃，缺失参数按默认，导入预设后显示具体的差异参数情况，只有预设导入有参数差异，如果一条参数也没有成功加载则显示加载失败。预设结构为json。可批量导入预设，导入完成后显示有参数差异、失败、成功的预设数量及其名称。如果导入时候找不到图，使用一个“图片缺失”占位。
            [组件]保存：将当前完整状态作为一条新预设加入预设列表，效果等同于导出+导入，只不过简化了导出导入的流程，直接把json数据新建了一个预设。鼠标在保存按钮上的时候，要提示保存仅本次有效。导出的预设json连同图片数据一同导出，导出时候将图片剪裁量化，只保存可见部分的图片数据。
            [组件]复制json：把json复制到剪切板。
            [组件]复制html：直接复制一整套 link 标签：包含 ico、apple‑touch‑icon 等标准网页标签文本。
            [组件]预设列表：开发适合预先设计几个典型预设，导入的预设不支持持久化，页面刷新后加载的预设消失。应按照预设的真实样式显示，点击切换预设后，内容参数、背景参数、预览应同步更新。预设展示的显示严格限制尺寸小于屏幕像素50px，防止显示过大挤占正常内容。每行根据标签内容内部宽度显示多个预设，仅显示预览图，不显示名称、删除按钮。点击预设的预览图就直接应用，预设列表后加载和渲染，不能影响主界面加载。
        2.2 [标签]下载历史列表：每次点击下载，会显示在这里，点击可快速恢复。历史数量保存最高20条全量快照，不保存本地数据，刷新后丢弃。右键或长按可显示删除按钮，点击删除按钮可删除。注意历史列表，是保存当前的数据json，并根据数据json绘制预览图，相当于每次下载出发了保存预设，但是是保存在历史列表中而非预设列表中。注意，对于上传图片的处理，历史列表中的图片是指向上传图片的id或引用，不直接保存图片数据。
    3. [模块]下载：
        3.1 [参数]透明色选项：勾选后默认白色为透明色导出，注明JPG不支持透明。
        3.2 [参数]导出尺寸设置：下拉快速选项+自定义输入框（输入<0时候为16，大于10000时候为8192），默认256*256，同时是画布尺寸。
            [组件]多尺寸打包：勾选后下载非矢量图时候下载的是zip打包的常见尺寸32、64、128、256。预留注意：即便勾选打包，如果版本支持矢量图，则矢量图只输出一份。
            [组件]代码打包：勾选后同时打包canvas代码、json预设。
            [组件]原始图片打包：勾选后，将上传的原始图片一并打包。
        3.3 [组件]下载格式: png、jpg、WebP、canvas（<canvas>标签+内联脚本，DPI固定为1x，导出里面画布尺寸是画布设置尺寸）、ico、json(等于导出预设)、html（直接复制一整套 link 标签：包含 ico、apple‑touch‑icon 等标准网页标签文本。）。点击即下载对应格式的文件或图片。svg较为复杂暂不支持。点击 ico 下载，输出单个多尺寸 ico 文件，内置 16/32/48/64/128/256，不受导出尺寸设置”参数影响；勾选打包时，导出ico和canvas代码、json预设为zip。
        3.4 导出的默认命名规则：KIcon-{尺寸}-{文本内容}-{日期时间}.扩展名。其中文本内容为所有行的文本拼接，FA使用FA代号，图片使用原图片名，多行文本中间无分隔符直接拼接，并过滤非法字符，对文本内容做总长度截断。时间日期格式为YYYYMMDDHHmmss。代码、ico、json、html、canvas、zip格式文件取消{尺寸}。zip内文件也按照这个规则命名。
        3.5 预留注意：如果方案定义支持svg，原生矢量 SVG 需要单独一套渲染逻辑，不能直接把 canvas 转 svg，避免svg下载图片与预览不一致。
        3.6 注意：辅助参考线不应被导出。
（二）内容参数栏：包含多行设置、单行内容和样式两个模块。
    1. [模块]多行设置
        1.1 [参数]行数选项：显示1行、2行~9行9个按钮。可切换文本行数，对应不同数量的单行内容和样式的标签。
        1.2 [参数]排版模式：
            1行：上半边、下半边、左半边、右半边、居中
            2行：全在上（左右分）、全在下（左右分）、全在左（上下分）、全在右（上下分）、左右分、上下分，
            3行：一字横排、一字纵排、品字（上1下2）、倒品（上2下1）
            4行：一字横排、一字纵排、四宫格排版、纵向121、横向121
            5行：一字横排、一字纵排，环形、四宫格+中央
            6行：一字横排、一字纵排、环形、2x3、3x2
            7行：一字横排、一字纵排、环形
            8行：一字横排、一字纵排、环形、2x4、4x2
            9行：一字横排、一字纵排、环形、3x3
        1.3 [参数]多行排列层次（仅行数>1时候出现）：选项「1 行‑>9 行」：绘制执行顺序 `9 →8 →7 … →1`，行 1 最后绘制，处于最顶层。选项「9 行‑>1 行」：绘制执行顺序 `1 →2 →3 … →9`，行 9 最后绘制，处于最顶层。
    2. [模块]内容：设置各行的内容，包含内容纵向标签组
        2.1 [标签组]单行内容：每行内容占据一个标签，组成并行标签组，多行采用纵向多标签组合的形式（左侧纵向标签，右侧激活标签的内容，意思是左侧从上到下显示1~9个标签标题，右边是显示当前激活标签的内容），标签标题的宽度默认为模块宽度的1/6，最大到模块宽度的1/4。
        2.2 [参数]单行内容模式：支持文本、图片、FontAwesome三种内容模式，直接在单行内容标签上进行选择。
        2.3 单行内容标签标题展示：标签标题分两个部分，前面一小块显示[组件]“文、图、F”按钮，表示当前选择的内容模式，点击可切换。后面显示内容的具体信息，其中文本模式下输入框放在单行内容和样式标签标题上，内容模式选择的后面，即可在标签上直接修改文本模式下的文本内容，让前端页面的布局更简洁。图片选择框、FontAwesome的标签仅展示，点击时候，在样式中展示上传（用于图片）和图标（用于FontAwesome）标签
        2.4 [组件]标签标题中，文本模式下的内容输入框：不限制输入框中字符串长度，字符串强制居中。超出输入框的可左右滚动。
        2.5 不同的内容模式下可设置不同的参数
        2.6 文本模式下可设置字体相关参数：[参数]中文字体、[参数]英文字体、[参数]粗细条件、[参数]斜体、[参数]排版（横排、纵排、环形向心）、[参数]字号。字体默认加载web字体,备选系统字体，预加载小体的积默认字体，其它字体按需加载。字体加载完成后再开始渲染，字体加载失败退回预设字体并提示原因，对于需要在线加载的字体要在字体下拉框中显示未下载字样，字体加载中则在预览图中显示字体加载中。环形向心是指多个文本沿圆环排列，圆环直径等于画布宽度。
        2.7 FontAwesome模式下可设置图标：在标签内容中，分类树状展示FontAwesome6的全部图标及其形状，支持搜索框搜索，搜索支持别名、关键词，选中即写回。数据全部本地打包，主界面加载后异步加载。在标签内增加FA6的商用版权提醒。页面最底部增加一行简短 FA6 版权提示.
        2.8 图片模式下的标签内容：[组件]打开按钮、[组件]图片预览（剪裁后的）、[组件]剪裁按钮（点击显示剪裁操作）。上传图片显示进度条，大于1MB时提醒用户会造成卡顿。更换图片时解除当前行对旧图片的引用，旧图片由会话图片仓库按引用计数延迟释放；切换行和模式不解除引用。动图取首帧，上传失败提示包含原因。
        2.9 快速复制样式：内容标签的标签内容最顶上有两个按钮，复制样式和黏贴样式，可以快速复制单行内容对应的全部样式模块包含的参数（即尺寸、颜色、阴影，不包括内容模块的参数）。跨模式复制只应用相同参数。
    3. [模块]样式：包含样式横向标签组，模块标题显示样式、当前激活的是第几行的参数，以及该行的模式和内容。
        3.1 [标签组]单行下的样式设置：包含标签（尺寸、颜色、阴影），横向组合，可平铺
        3.2 参数内容：可设置的参数跟随内容模块中激活的标签变化，即当内容模块中选择行1标签时候，显示行1标签的相关配置。
        3.3 不同模式下有不同的参数，行被减去增加后其参数和模式不必重置，非本模式时候不显示非本模式的参数并在切换模式时候保留数据。各标签下显示重置按钮。
        3.4.[标签]颜色：标签标题上显示颜色，文本和FA模式下，包含[参数]颜色模式：单色、渐变，[参数]颜色选择器（跟随颜色模式有1和2个选择器的变化），[参数]颜色建议：根据当前设置的背景色提供互补、类似、柔和、明亮的颜色建议（根据颜色使用公式计算），颜色建议随背景色同步变化，颜色建议是实时计算，背景的颜色参数修改后要主动触发颜色建议重算。图片模式下颜色只有一个[参数]选项：设置图片中的白色为透明，每个图片模式的行独立开关。
        3.5 [标签]尺寸：不同模式均包含同一批参数           
            [参数]大小：不限制内容的大小尺寸，可设置：1%~300%。
            [参数]角度：旋转内容0~360度,旋转中心为内容/图片/图标的中心。
            [参数]水平拉伸：默认100%，可设置：1%~300%。拉伸不改变渐变效果。
            [参数]垂直拉伸：默认100%，可设置：1%~300%。拉伸不改变渐变效果。
            [参数]横向偏移：默认0，范围-100%~100%,右边超出左画布~左边超出右画布。
            [参数]纵向偏移：默认0，范围-100%~100%,上边超出下画布~下边超出上画布。
            [参数]显示超出形状范围的内容：若否，则字体内容超出边框范围的部分，即形状边框内的可见，边框外的不可见、根据形状边框被裁切。
        3.6 [标签]阴影：标签标题上显示绿色对号以表示启用，包含[参数]：启用、颜色、大小、模糊、X偏移、Y偏移。
        3.7.[组件]参数联动：所有参数设置，每个参数后面加一个图标表示参数联动（断开和合上的链条），开启任意参数的联动则启用全部参数联动，其它所有行的该参数联动同步被启用，意味着调整时候将其它所有行的该参数一起联动调整。联动打开后，某个行的参数联动被取消时候是单独取消，只取消这个行的这个参数的联动。举例：行1~6均包含大小参数，默认全部不勾选，当勾选行1大小参数的联动，所有行的行1的大小参数后的联动选框被一起勾选，此时调整任意行的大小参数，所有行的大小参数同步变化。接着取消勾选行1的大小参数后的联动选框，此时调整行1的大小，因为联动选框未被勾选，因此其它行的大小参数不变化。如果有参数被勾选联动，新增行也同时联动。因为行数变动导致行被取消显示的时候，其联动状态不变。布尔类型也要有联动。
        3.8 [组件]参数显示和输入框：所有参数后面，使用输入框显示当前参数，允许用户直接输入调节参数。
（三）背景参数栏
    1. [模块]形状和外框：
        1.1 [标签]形状：
            1.1.1 [参数]形状，其中因为参数较多，分为基础、边形、角形三个参数类，每个参数类有多个按钮，占据一行，以类名+按钮形式出现。
                [参数类]基础：无、圆形、圆角方形。关联的参数：尺寸（默认尺寸应使长边等于绘图画布尺寸）、拉伸、方向、弧度（圆角方形）
                [参数类]边形（3~8）：关联的参数：尺寸、拉伸、方向、弧度（弧度100%时候应为圆形）
                [参数类]角星（3~8）：关联的参数，在边形参数上增加内角滑块（改变内角角度，内角为0的时候，背景形状应当消失，默认内角60度，UI 上滑块允许滑到 0，但是渲染消失，在此处红字提示用户 “内角 0° 形状不可见）
            1.1.2 [参数]形状参数：根据选择的形状动态变化。默认值：方向和弧度是0。方向=0时候，应为使形状最下面的边水平。形状大小增加“填满”按钮，使形状填满绘图区域。
            1.1.3 注意：外框形状应当为封闭的连续连线，连线禁止交叠，使外框形状天然分为内外两个部分。
        1.2 [标签]边框：[参数]启用边框，启用后在标签标题上显示绿色对号，设置[参数]边框宽度和[参数]颜色。注意边框从形状边框向外延伸。
        1.3 [标签]阴影：[参数]启用形状阴影，启用后在标签标题上显示绿色对号，设置[参数]大小、[参数]模糊、[参数]XY方向。
    2. [模块]填充数量和布局：用于选择形状填充内容的数量、布局和边界
        2.1 [参数]填充数量：六个按钮：单色、二色、三色、四色、五色、六色。数量切换时候动态改变内部填充模块的标签数量为对应数量。数量增减时候标签显示或取消显示，但其内部参数不需要重置。
        2.2 [标签组]布局和边界，单色不出现，仅多拼色下出现，用于设置多个拼色色块的组合形式、边界样式。
        2.3 [标签]布局：设置多个拼色色块的组合形式。
                [参数]布局形式：矩阵布局（所有颜色在二位平面的单元格中填充）、饼图布局（所有颜色以一个中心点按角度划分填充），所有布局都是只有内部色块分界无外部边框的，因此任意情况、参数、布局下颜色都可以让整个形状被填充。
                [参数]层数：矩形布局时候，为纵向层数，例如六色情况下设置层数2，则显示一个纵向2层横向3列（总色块6/层数2=每层色块3）的矩形方框，每个方框填充一个颜色，设置层数1，则六个颜色在横向一字排开，纵向无限制。饼图布局时候，例如层数2则显示双层饼图，每层色块数量为总色块6/层数2=3，因此外层3色内层3色，外层无限制。取值范围1~当前填充数量，填充数量降低时候跟随降低，默认为1。色块数量无法均分时候分给最外层、最上层。
                [参数]层间比例：通过更改多色色块的分界线偏移修改层间色块面积比例。例如四色矩阵布局下，层为2时候，十字划分形状为四个色块，因此显示一个层间比例，修改则从视觉上看是十字分界的横线上下移动。例如六色饼图布局下，显示两层饼图，有一个层间比例，修改到最小时候，内层消失，修改到最大则外层颜色消失。只有层数大于1时候才显示层间比例参数，多个层间比例是显示在一个滑轨上的多个滑块，滑块数量对应分界线的数量，分别控制对应的分界线。后面显示多个参数输入兼显示框，有均分按钮，点击均分，默认也是均分。
                [参数]层内比例：通过更改多色色块的分界线偏移修改层内色块面积比例。例如四色矩阵布局下，层为2时候，十字划分形状为四个色块，因此显示一个层内比例，修改则从视觉上看是十字分界的竖线左右移动。层1时候，显示三个层内比例，调节四色之间三个分界线的位置。例如六色饼图布局下，每层有三色，因此显示三个层内比例，调节实际上是调节饼图单层内三个分界线的角度。只有单层内色块数量大于1时候才显示层内比例参数，多个层内比例是显示在一个滑轨上的多个滑块，滑块数量对应分界线的数量，分别控制对应的分界线。后面显示多个参数输入兼显示框，有均分按钮，点击均分，默认也是均分。
                [参数]方向：旋转整个填充。饼图布局且层数大于等于2时，为每层设置一个独立的方向参数，方向参数也采用多滑块共享滑轨的方式，层数等于滑块数量。旋转中心为内容/图片/图标的中心。
                [参数]偏移：X方向整体偏移，Y方向整体偏移
                [参数]拉伸：X方向拉伸，Y方向拉伸。
        2.3 [标签]填充边界：设置色块边界的形状和过渡样式。
                [参数]过渡宽度：默认0，表示无过渡。
                [参数]边界形状：直线、sin、tan、锯齿。tan和sin的圆点均为中心点。
                [参数]边界参数：选择sin和tan的时候可配置y=Asin(ωx+φ)+k、y=Atan(ωx+φ)+k中的A、ω、φ、k。参数输入应有合法数值范围限制，防止除零、无穷大导致渲染崩溃，渲染也做截断保护，超出画布大小10倍后截断。锯齿可设置A高度，ω宽度，锯齿原点以形状中心点为基准。注意，不是输入A, ω, φ, k这四个字母，是显示A, ω, φ, k这四个边界参数（开发时候按函数安全给定范围），参数有滑块、参数值显示兼输入框、联动、重置等按钮。
                [参数]过渡样式：单色、渐变、加深、变浅、透明。
        2.4 [标签]颜色建议：根据文本颜色，提供多色块的组合颜色建议，如互补、类似、柔和、明亮的颜色建议，直接设置填充模块内各色块的纯色背景、渐变色背景。例如选择六色情况下，提供六个互补色、六个类似色、六个柔和色、六个明亮色，用户选择一个可直接填充色块。一键填充仅限制纯色。
    3. [模块]内部填充
        3.1 [标签组]多标签形式，纵向布局，标签标题为颜色编号，标签底色为标签canvas兼顾颜色预览，标签宽度与高度相同为方向，高度为两倍文本高度。标签组中的标签数量等于激活的[参数]填充数量。每个标签根据其背景模式可以独立调节参数，每个标签可以独立设置背景模式。
        3.2 [参数]背景模式可切换：纯色、渐变、图片，背景模式的切换以按钮形式显示在标签标题的左上角（纯、渐、图），每个背景模式都有其独立的参数体系。每个色块可以独立切换背景模式，因此每个色块可以独立设置纯色的颜色、渐变、图片。
        3.2.1 纯色背景模式下的参数：颜色选择器、颜色建议（根据文本颜色提供互补、类似、柔和、明亮的颜色建议）
        3.2.2 渐变背景模式下的参数：渐变颜色选择（颜色选择器、颜色建议）、渐变模式选择（线性渐变、径向渐变）、渐变角度滑块。
        3.2.3 图片背景模式下的参数：图片上传选择器，大小和位置调整滑块，[组件]图片预览（剪裁后的）、[组件]剪裁按钮（点击显示剪裁操作）。背景填充全部被外框形状裁切。更换图片时减少旧图片引用计数，按会话图片仓库规则延迟释放。
（四）顶部工具栏：
    1. [组件]显示标题、作者（Kong）
    2. 动作历史（撤销栈）
        栈：动作历史中保存最高20条全量快照（根据设备动态降低上限），记录json注册数据的变动，FIFO淘汰.
        产生历史：上传的图片切换、预设切换、参数联动变更、参数变更（滑块拖动结束、输入框失去焦点）等等凡在json中注册的参数变更都计入历史。
        不生产动作历史：预览拖动、滑块拖动鼠标未释放时导致的参数变化、下载动作（因为会进入下载历史列表）、界面样式。
        [按钮]撤销、重做按钮：在顶部工具栏显示。 
        撤销后如果人工修改，后面的重做失效。被覆盖的快照如果包含图片引用应触发引用注销。
    3. [按钮]参数联动：根据参数联动的情况，显示当前的参数联动状态（全部参数联动、部分参数联动、无参数联动），点击可切换为（全部参数联动、无参数联动），同时设置或取消所有可联动参数。 生效范围包括所有可见和不可见的行。注意，这是批量改写每一行每个参数的联动标记，改写完成后各行链条图标同步更新；改写完成后用户仍然可以单独对某行某参数取消或设置联动，这个联动状态也会同步被触发更新。      
（五）快捷键：
    ctrl+z：撤销。注意：当输入框 /textarea 获得焦点，交给浏览器原生文本撤销，不应触发工程撤销重做。
    ctrl+y：重做
（六）性能和内存优化
    1. 会话图片仓库：上传图片只生成一份资源，所有行、背景、历史列表、预设列表、动作历史只引用。加载预设后，预设列表中预设包含的图片数据被自动剥离加载注册到图片仓库，预设列表中预设包含的图片变更为引用。应有引用计数（图片参数、预设、历史下载、动作历史撤销栈都计数），无引用时释放缓存。所有引用图片仓库的参数变动时候都应注销或增加其引用。

三、界面要求

1. 界面样式：标题后面界面样式选择按钮，提供明亮（白底白色背景、灰色框、浅彩色块填充标签）、复古（黄底）、暗黑（黑色背景）三种前端界面，默认为明亮。
2. 自适应界面：
    2.1.模块可以根据视口大小压缩、折叠和大小调整 
    2.2.默认视口大小：800*400，视口在这个尺寸以及以上，无滑块，随视口变大可展开更多设置参数。
    2.3.视口小于默认视口尺寸时候，可以产生纵向滑块，但预览界面应当固定显示
    2.4 默认视口布局为左中右三栏等分结构，视口变窄时候将中、右栏以标签形式合并为一栏，进一步变窄时候变为纵向滑动结构。
    2.5 纵向结构:手机布局下、视口变窄时候采用，其中预览模块应当固定浮动显示在视口顶部，其余模块纵列显示在预览界面下方滑动显示。所有标签组均合并。
    2.6 手机进入横排时候，进入三栏模式。
    2.7 单栏在纵向视口不充足时候，优先进行标签合并，没有标签可合并时候，加入栏内上下滚动
    2.8 标签组内部标签的自适应展开和组合：如果纵向视口充足，尺寸、颜色、阴影全部展示，纵向分布。如果不充足，按标签（如尺寸、颜色、阴影）标签组展示。注意是根据模块内可用高度而不是全局视口。全局视口压缩栏高度，栏高度压缩模块高度，模块高度导致标签自适应组合。如果标签组合后无法压缩，应反压模块高度，模块高度反压栏高度，栏高度被反压且无空间可用时候，启用纵向滑块。
    
3. 交互和UI要求
     所有参数允许直接输入设置（做好输入合法校验），输入框应与现存的显示数据合二为一。
     非法输入应先变输入框底色为浅红色，闪烁两次，然后将参数值设置为距离非法输入最近的合法数值，设置后输入框底色恢复。
     点击激活的标签与其参数区域背景设置为浅蓝色。激活的标签应当和下面的参数区域连在一起，中间没有边框阻隔，表示这是一体的。
     标签功能被启用时候，应在标签上显示。如：形状非无时候，在标签上显示形状。阴影和边框启用时，标签上显示绿色对号。
     图片剪裁器：缩放、拖拽、快速比例。

4.按照栏、模块、参数标签、组件和参数分四层组织
    栏: 默认三栏，根据视口调整，栏只有栏标题（顶部），无可视边框，无底色。
    模块：基本组织单元、滚动移动单元，有模块标题（标题栏与模块宽度相同）、模块内容（包含参数标签、参数、组件）、模块边框、模块底色（默认透明）。模块在栏中纵向排列。
          通用模块样式如下：
```通用模块样式：   
             ------------------
             |   模块标题       |
             |-----------------| 
             |   模块内容       |
             ------------------
```

    参数标签：以标签形式展示各类设置参数，是同类型参数、组件的容器，可被合并、展开和折叠，有横向标签和纵向标签两种多标签组织形式。包含标签标题、内容、边框（灰色低可视度框，与模块边框做区分）、底色（默认浅蓝色）。标签标题会展示其包含的主要参数，也可设置其主要参数。标签内容中允许嵌套标签组。
    标签聚合在一起时候称为标签组，标签组无可见形态，标签组为多个标签的布局和样式容器，多个标签排列时候即为标签组。
    标签组有两种情况，一个是唯一标签组，二是并行标签组。唯一标签组是指，标签组承担参数功能，选择哪个标签就激活哪个标签的功能，使能该标签包含的参数，因此切换标签也承担了选择功能，并行标签组是指多标签的参数同时可用。唯一标签组平铺拆分没有意义，因此不会随视口平铺拆分，并行标签组组合是为了布局紧凑，因此其包含的标签在视口富裕的时候可以被横向或纵向平铺。两种标签组在UI上没有区别。
    当模块空间足够时候，采用平分法拆解标签。例如：当前有包含六个内容标签的标签组，模块空间纵向足够放当前标签组高度两个，就分成两个标签组，每个标签组有两个标签。当纵向空间足够放3个标签组，就分成三个标签组，每个标签组2个标签。
    标签组拆分合并不影响当前的激活状态。被激活的标签保持与未激活标签不同的颜色。
            单个标签样式如下：

```单个标签样式
               -------                                
              /标签标题\                           
              |        -----------------            
              |         标签内容        |              
              --------------------------               
```
            多个标签聚合为标签组，且横向排列时样式如下：
```
               -------  -------  -------    
              /当前标签\/其它标签\/其它标签\  
              |        -------------------
              |         标签内容          |
              ----------------------------
```
            多个标签聚合为标签组,纵向排列时样式如下：
```
              ------------------------------
              |当前标签                       |   
              |---------|                    |
              |其它标签  |                    |
              |---------|       标签内容      |
              | 其它标签 |                    |
              |---------|                    |
              | 其它标签 |                    |
              --------------------------------
```
        标签组可根据模块的横向和纵向视口，将其标签自动合并和同时排列。当横向视口充足时，标签可横向展开
```
                 -------                   -------                   
                /标签标题\                 /标签标题\                   
                |        ---------------- |        -----------------  
                |         标签内容        ||         标签内容        |  
                -------------------------- ------------------------- 
```
        当纵向视口充足时，标签可自动纵向展开
```
                  -------                   
                 /标签标题\                 
                 |        ----------------- 
                 |         标签内容        |
                 --------------------------        
                  -------                   
                 /标签标题\                 
                 |        ----------------- 
                 |         标签内容        |
                 -------------------------- 
```
        当横向和纵向视口都充足时，标签可自动在两个方向展开
```
                  -------                     -------                   
                 /标签标题\                   /标签标题\                  
                 |        -----------------  |        -----------------  
                 |         标签内容        | |         标签内容        |  
                 --------------------------  --------------------------       
                  -------                     -------                    
                 /标签标题\                   /标签标题\                  
                 |        -----------------  |        ----------------- 
                 |         标签内容        |  |         标签内容        |  
                 --------------------------  --------------------------   
```
    组件和参数：
        组件：基本功能组件，如按钮、预览等
        参数：被设置的基础参数，一般为一个参数一行，参数名在前，参数滑块、选项、按钮、输入框、数值等组件在后。
        拖动滑块时，同步更新对应输入框显示数值
        参数类：当同一个参数有过多选项时候，可以使用参数类将参数分为多行显示，每行一类，类名在行前。


四、技术开发流程
    根据需求进一步完善各模块、参数、功能点的方案。
    针对各功能点制定开发验收测例。
    对各参数制定合法校验公式、上下限的范围。
    制定数据的JSON schema定义，预留可扩展性。包括：所有绘图参数、图片数据（引用或数据）、参数联动
    会话级图片仓库：完成字段定义。