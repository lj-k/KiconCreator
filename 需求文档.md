一、产品定位
名字：图标王制作器，K
零安装、全浏览器运行的图标制作工具

二、功能需求
结合fuwa.org的 和 icon.html的功能。
按照内容、背景分开设置。

按照栏、模块、功能区、设置标签分四级组织。

（一）预览预设下载栏，包含预览、预设、下载三个模块
    1. 预览模块：
        1. 只显示当前预览，不显示多尺寸预览。
        2. 竖屏情况下，保持位置在视口上方浮动固定
    2. 预设模块：
        2.1 包含包括内容、背景在内的预设。预设可导出和加载，加载后在预设中显示。
        2.2 预设应按照预设的样式显示，切换预设后，内容参数、背景参数、预览应同步更新。预设显示严格限制尺寸小于50px，防止显示过大挤占正常内容。
    3. 下载模块：
        3.1 透明色选项（勾选后默认白色为透明色导出），设置导出尺寸，下载格式
        3.2 导出图片的默认命名规则：IconKing-{尺寸}-{文本内容}-{日期时间}.扩展名。其中文本内容为所有行的文本拼接。
（二）内容参数栏
    1. 多行设置功能区
        1.1 行数选项：可增加文本行数，最多6行，每增加一，增加一行，增加一个文本样式标签
        1.2 排版模式：
            1行：上半边、下半边、左半边、右半边、居中
            2行：全在上（左右分）、全在下（左右分）、全在左（上下分）、全在右（上下分）、左右分、上下分，
            3行：一字横排、一字纵排、品字、倒品
            4行：一字横排、一字纵排、四宫格排版、纵向121、横向121
            5行：一字横排、一字纵排，环形
            6行：一字横排、一字纵排、环形
    2. 单行内容和样式功能区（多标签形式）
        2.1 支持文本、图片、FontAwesome三种内容模式，在单行内容和样式功能区标签上进行选择。标签分两个部分，前面一小块显示“文、图、F”，表示当前选择的内容模式，点击可切换。后面显示内容的具体信息。
        2.2 文本输入框放在单行内容和样式功能区的各行标签上，内容模式选择的后面，标签和输入框二合一，让前端页面的布局更简洁。
        2.3 图片选择框、FontAwesome的标签仅展示，点击时候，在样式中展示上传（用于图片）和图标（用于FontAwesome）标签
        2.2 文本模式：不限制输入框中字符串长度，超出宽度的文本不管，字符串强制居中
        2.3 不限制内容的大小尺寸，超出不管
        2.4 文本模式下样式可选：字体、尺寸、颜色、阴影；FontAwesome模式下样式可选：图标、阴影、尺寸；图片模式下可选上传、阴影、尺寸。不可选的就不显示了。
        2.5.颜色：支持单色、渐变、拼色填充，颜色选择上，根据当前设置的背景色提供互补、类似、柔和、明亮的颜色建议，颜色建议随背景色变化。
        2.6 尺寸：应具备默认尺寸和大小，偏移为0，拉伸为100%。具备选项：超出形状边框的部分是否显示。决定字体内容超出边框范围的部分是否可见。
        2.6.所有参数设置，每个参数后面加选框[]联动，勾选则启用全部联动，调整时候连同其它行一起联动调整。
        2.7. 如果纵向视口充足，字体、尺寸、颜色、阴影全部展示，纵向分布。如果不充足，按分类（字体、尺寸、颜色、阴影）标签化展示。
        2.8 FontAwesome选择时候，应当分类树状展示全部图标，同时展示形状。
（三）背景参数栏
    1. 形状和外框功能区：
        1.1 形状标签：
            1.1.1 基础形状：无、圆形、圆角方形。包含参数：尺寸（默认尺寸应使长边等于绘图画布尺寸）、拉伸、方向、弧度（圆角方形）
            1.1.2 边形（3~8）：包含参数：尺寸、拉伸、方向、弧度（弧度100%时候应为圆形）
            1.1.3 角星（3~8）：增加角度滑块（改变内角角度，内角为0的时候，背景形状应当消失，默认内角60度）
            1.1.4 形状参数：默认角度和弧度是0。方向=0时候，应为使下边平行的角度。形状大小增加“填满”按钮，使形状填满绘图区域。
        1.2 边框标签：设置边框宽度和颜色
        1.3 阴影标签：设置阴影大小、模糊、XY方向
    2. 填充边界和布局功能区（位置在拼色选项和内部填充功能区之间）
        2.1 填充内容之间的布局和边界可设置
        2.2 填充内容布局设置：任意数量情况下一字布局（配置角度和面积比例），三拼色情况下品字布局（角度，纵横两个面积比例），四拼色情况下四宫格布局（角度和纵横两个比例）。面积比例：定义不同颜色的占比，即颜色分界线的偏移位置。
        2.3 填充边界设置：边界形状设置（直线、曲线：支持sin、tan函数及其参数），拼色形状选择（直线、曲线），拼色过渡（叠加、加深、渐变、变浅、不透明、透明），拼色过渡宽度（0表示无过渡）
        2.4 快速颜色建议（根据文本颜色提供互补、类似、柔和、明亮的颜色建议，直接设置填充标签的纯色背景、渐变色背景）
    3. 内部填充功能区（多标签形式，最多配置四个填充标签：单色、二分拼色、三分拼色、四分拼色）
        3.1 标签canvas兼顾预览，限制尺寸小于模块宽度的1/4，小于100px，以免过大占据视口。
        3.2 每个填充标签可选多个填充内容：纯色、渐变、图片。
        3.2.1. 纯色背景：颜色选择器、颜色建议（根据文本颜色提供互补、类似、柔和、明亮的颜色建议）
        3.2.2 渐变背景：渐变颜色选择（颜色选择器、颜色建议）、渐变模式选择（线性渐变、径向渐变）、渐变角度滑块。
        3.2.3. 图片背景：图片上传选择器，大小和位置调整滑块。
        
三、界面要求
1. 界面样式：提供明亮（白底白色背景、灰色框、浅彩色块填充模块）、复古（黄底）、暗黑（黑色背景）三种前端界面，默认为明亮。
2. 自适应界面：
    1.1.模块可以根据视口大小压缩、折叠和大小调整 
    1.2.默认视口大小：800*400，视口在这个尺寸以及以上，无滑块，随视口变大可展开更多设置参数。
    1.3.视口小于默认视口尺寸时候，可以产生纵向滑块，但预览界面应当固定显示
3. 界面布局
    2.1 默认视口布局为左右三列等分结构，左侧显示预览、下载、导出，中列显示参数模块。
        布局
            左栏              中栏 （ 内容参数模块，文本图标模式）   右栏（背景参数模块）
        |    预览块          |  多行文本选项和排版              |    图标尺寸、形状、大小           |  
        | 预设样式选择块      |  标签切换设置具体单行文本样式    |     颜色、渐变、拼色样式、多色预设  |  
        | 下载导出查看等按钮  |                                |     透明色、边框、阴影             | 
    2.2 手机布局下为纵向结构，预览图应当固定显示在视口顶部，参数模块显示在预览界面下方滑动显示。
4. 交互和UI要求
     所有参数允许直接输入设置（做好输入合法校验），输入框应与现存的显示数据合二为一
     点击激活的标签与其参数区域背景设置为浅蓝色。激活的标签应当和下面的参数区域连在一起，中间没有边框阻隔，表示这是一体的。
     标签功能被启用时候，应在标签上显示。如：形状非无时候，在标签上显示形状。阴影和边框启用时，标签上显示绿色对号。
