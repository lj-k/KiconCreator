一、产品定位
名字：图标制作器，IconKing
零安装、全浏览器运行的文本图标制作工具

二、功能需求
结合fuwa.org的文字图标功能 和 icon.html
按照内容、背景分开设置。

（一）预览模块，包含预览、预设、下载三个功能区
    1. 预设模块包含包括内容、背景在内的预设
    2. 预览模块只显示当前预览，不显示多尺寸预览。
    3. 预设应按照预设的样式显示。
    4. 导出图片的命名规则：IconKing-{尺寸}-{文本内容}-{日期时间}.扩展名
（二）内容参数模块
    1. 多行设置功能区
        1.1 文本行数选项：可增加文本行数，最多4行，每增加一，增加一行，增加一个文本样式标签
        1.2 多行文本排版设置：1行（横排上半边、横排下半边、横排剧中、纵排左半边、纵排右半边、纵排居中），2行（左右纵向排版、上下横向排版），3行（品字排版、倒品排版），4行（四宫格排版、121排版）
    2. 单行内容和样式功能区（多标签形式）
        2.1 支持文本、图片、FontAwesome三种内容，在单行内容和样式功能区标签上进行选择。标签分两个部分，前面一小块显示“文、图、F”，表示当前选择的内容模式。后面显示内容的具体信息。
        2.2 文本输入框、图片选择框、FontAwesome选择框都放在单行内容和样式功能区的各行标签上，内容模式选择的后面，标签和输入框二合一，让前端页面的布局更简洁。
        2.2 文本模式：不限制输入框中字符串长度，超出宽度的文本不管，字符串强制居中
        2.3 不限制内容的大小尺寸，超出不管
        2.4 支持设置阴影
        2.5.填充色：支持单色、渐变、拼色填充，颜色选择上，根据背景色提供互补、类似、柔和、明亮的颜色建议。
（三）背景参数模块
    1. 外框形状功能区：圆形、圆角矩形（仅此选项显示弧度滑块）、正方形、胶囊形、八边形、菱形
    2. 内部填充功能区（多标签形式，最多配置四个填充标签：单色、二分拼色、三分拼色、四分拼色）
        2.1 填充内容之间的布局和边界可设置
        2.1.1 填充内容布局设置：任意数量情况下一字排列（配置角度和面积比例），三拼色情况下品字排列（角度，纵横两个面积比例），四拼色情况下四宫格排列（角度和纵横两个比例）
        2.1.2 填充边界设置：边界形状设置（直线、曲线），拼色形状选择（直线、曲线），拼色过渡（叠加、加深、渐变、变浅、不透明、透明），拼色过渡宽度（0表示无过渡）
        2.1.3 快速颜色建议（根据文本颜色提供互补、类似、柔和、明亮的颜色建议，直接设置填充标签的纯色背景）
        2.2 每个填充标签可选多个填充内容：纯色、渐变、图片
        2.2.1. 纯色背景：颜色选择器、颜色建议（根据文本颜色提供互补、类似、柔和、明亮的颜色建议）
        2.2.2. 渐变背景：渐变颜色选择（颜色选择器、颜色建议）、渐变模式选择（线性渐变、径向渐变）、渐变角度滑块。
        2.2.3. 图片背景：图片上传选择器，大小和位置调整滑块。
        
    3. 边框参数功能区（左右布局）：支持设置边框和背景阴影，以标签形式设置
    4. 高级选项功能区：透明色选项（勾选后默认白色为透明色）


三、界面要求
1. 界面样式：提供明亮（白底白色背景、黑框、彩色块填充）、复古（黄底）、暗黑（黑色背景）三种前端界面
2. 自适应界面：
    1.1.模块可以根据视口大小压缩、折叠和大小调整 
    1.2.默认视口大小：800*400，视口在这个尺寸以及以上，无滑块，随视口变大可展开更多设置参数。
    1.3.视口小于默认视口尺寸时候，可以产生纵向滑块，但预览界面应当固定显示
3. 界面布局
    2.1 默认视口布局为左右三列等分结构，左侧显示预览、下载、导出，中列显示参数模块。
        布局
            左栏              中栏 （ 内容参数模块，文本图标模式）   右栏（背景参数模块）
        |    预览块          |  多行文本选项和排版              |    图标尺寸、形状、大小           |  
        | 预设样式选择块      |  标签切换设置具体单行文本样式    |     颜色、渐变、拼色样式、多色预设  |  
        | 下载导出查看等按钮  |                                |     透明色、边框、阴影             | 
    2.2 手机布局下为纵向结构，预览图应当固定显示在视口顶部，参数模块显示在预览界面下方滑动显示。
